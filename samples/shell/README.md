# Blaster Handler in Shell

Sample blaster handler written in shell script ([credit](https://github.com/benrady/shinatra)) 

## How to run
```
blaster sqs --region <sqs region> --queue-name <queue name> --handler-command ./start.sh
```
