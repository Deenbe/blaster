# Blaster Handler in Javascript

Sample blaster handler written in javascript.

## How to run
```
npm install
blaster sqs --region <sqs region> --queue-name <queue name> --handler-command ./index.js
```
